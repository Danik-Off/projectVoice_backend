# Быстрый старт на Windows 🪟

Пошаговая инструкция для быстрого развертывания проекта ProjectVoice на Windows для разработки.

## 📋 Предварительные требования

### 1. Установка Node.js

1. Скачайте Node.js LTS версию с официального сайта: https://nodejs.org/
2. Запустите установщик и следуйте инструкциям (оставьте все галочки по умолчанию)
3. Проверьте установку в PowerShell или CMD:

```powershell
node --version
npm --version
```

Должны отобразиться версии (например, v18.x.x и 9.x.x)

### 2. Установка MySQL

**Вариант 1: MySQL Installer (рекомендуется)**

1. Скачайте MySQL Installer: https://dev.mysql.com/downloads/installer/
   - Выберите "MySQL Installer for Windows"
   - Рекомендуется версия "mysql-installer-community"
2. Запустите установщик
3. Выберите "Developer Default" или "Server only"
4. Настройте root пароль (запомните его!)
5. Запустите MySQL как службу Windows

**Вариант 2: XAMPP (проще для начинающих)**

1. Скачайте XAMPP: https://www.apachefriends.org/
2. Установите XAMPP
3. Запустите XAMPP Control Panel
4. Нажмите "Start" для MySQL

### 3. Установка Git (если еще не установлен)

1. Скачайте Git: https://git-scm.com/download/win
2. Установите с настройками по умолчанию

## 🚀 Быстрая установка проекта

### Шаг 1: Клонирование проекта

Откройте PowerShell или CMD в папке, где хотите разместить проект:

```powershell
# Перейдите в нужную папку (например, Documents)
cd C:\Users\ВашеИмя\Documents

# Клонируйте репозиторий (замените URL на ваш)
git clone <your-repository-url>
cd projectVoice_backend
```

Или если проект уже скачан, просто перейдите в папку проекта.

### Шаг 2: Установка зависимостей

```powershell
npm install
```

Это займет несколько минут. Дождитесь завершения установки.

### Шаг 3: Настройка базы данных

#### Создание базы данных через MySQL Workbench (проще)

1. Откройте MySQL Workbench (устанавливается вместе с MySQL)
2. Подключитесь к localhost (root пользователь)
3. Выполните следующие SQL команды:

```sql
-- Создание базы данных
CREATE DATABASE test_projectvoice CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- Создание пользователя
CREATE USER 'test_projectvoice'@'localhost' IDENTIFIED BY 'TZ8TSEWPet5kJnzN';

-- Предоставление прав
GRANT ALL PRIVILEGES ON test_projectvoice.* TO 'test_projectvoice'@'localhost';
FLUSH PRIVILEGES;
```

#### Или через командную строку MySQL

```powershell
# Подключение к MySQL (введите root пароль)
mysql -u root -p

# Затем выполните SQL команды выше
```

### Шаг 4: Настройка переменных окружения

1. Скопируйте файл `.env.example` в `.env`:

```powershell
copy env.example .env
```

2. Откройте файл `.env` в любом текстовом редакторе (Notepad++, VS Code, или обычный Блокнот)

3. Убедитесь, что настройки соответствуют вашей базе данных:

```env
# Database Configuration
DB_USERNAME=test_projectvoice
DB_PASSWORD=TZ8TSEWPet5kJnzN
DB_DATABASE=test_projectvoice
DB_HOST=localhost
DB_PORT=3306
DB_DIALECT=mysql
DB_LOGGING=false

# Server Configuration
PORT=5001
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
```

**Важно:** Если вы использовали другой пароль для MySQL пользователя, измените `DB_PASSWORD` в `.env` файле.

### Шаг 5: Запуск миграций базы данных

```powershell
npm run db:migrate
```

Должно появиться сообщение о успешном выполнении миграций.

### Шаг 6: Создание администратора

После успешного выполнения миграций создайте администратора:

```powershell
npm run create-admin
```

Это создаст администратора с учетными данными:
- **Email:** `admin@projectvoice.com` (или из `.env` файла)
- **Username:** `admin` (или из `.env` файла)
- **Password:** `admin123` (или из `.env` файла)

**Важно:** Измените пароль администратора после первого входа!

Вы можете настроить учетные данные администратора в файле `.env`:
```env
ADMIN_USERNAME=admin
ADMIN_EMAIL=admin@projectvoice.com
ADMIN_PASSWORD=your_secure_password_here
```

### Шаг 7: Запуск сервера

**Режим разработки (с автоперезагрузкой):**

```powershell
npm run dev
```

Или **обычный режим:**

```powershell
npm start
```

Вы должны увидеть:
```
🚀 Сервер запущен на порту 5001
📚 Swagger документация: http://localhost:5001/api-docs
🌐 API базовый URL: http://localhost:5001/api
```

## ✅ Проверка работоспособности

### 1. Проверка API документации

Откройте в браузере: http://localhost:5001/api-docs

Должна открыться страница Swagger UI с документацией API.

### 2. Проверка API эндпоинта

Откройте в браузере или используйте PowerShell:

```powershell
# Проверка через браузер
# Откройте: http://localhost:5001/api-docs

# Или через PowerShell (Invoke-WebRequest)
Invoke-WebRequest -Uri http://localhost:5001/api-docs
```

### 3. Тест регистрации пользователя

Используйте Postman, Insomnia или curl:

```powershell
# Установка curl (если не установлен)
# Скачайте: https://curl.se/windows/

# Тест регистрации
curl -X POST http://localhost:5001/api/auth/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"testuser\",\"email\":\"test@example.com\",\"password\":\"password123\"}"
```

## 🔧 Устранение проблем

### Проблема: "Cannot connect to MySQL"

**Решение:**
1. Убедитесь, что MySQL запущен:
   - Откройте "Службы" (Services) через Win+R → `services.msc`
   - Найдите "MySQL80" или "MySQL" и проверьте, что статус "Выполняется"
   - Если не запущен, нажмите "Запустить"

2. Проверьте подключение:
```powershell
mysql -u test_projectvoice -p
# Введите пароль: TZ8TSEWPet5kJnzN
```

### Проблема: "Port 5001 is already in use"

**Решение:**
1. Найдите процесс, использующий порт:
```powershell
netstat -ano | findstr :5001
```

2. Завершите процесс или измените порт в `.env`:
```env
PORT=5002
```

### Проблема: "Module not found" или ошибки установки

**Решение:**
```powershell
# Очистка кэша и переустановка
npm cache clean --force
rm -r node_modules
rm package-lock.json
npm install
```

### Проблема: "Sequelize connection error"

**Решение:**
1. Проверьте настройки в `.env` файле
2. Убедитесь, что база данных создана:
```sql
SHOW DATABASES;
```
3. Проверьте права пользователя:
```sql
SHOW GRANTS FOR 'test_projectvoice'@'localhost';
```

### Проблема: PowerShell не распознает команды

**Решение:**
- Используйте CMD вместо PowerShell
- Или установите Git Bash: https://git-scm.com/download/win

## 📝 Полезные команды

```powershell
# Запуск в режиме разработки
npm run dev

# Запуск миграций
npm run db:migrate

# Создание администратора
npm run create-admin

# Проверка статуса миграций
npm run db:migrate:status

# Откат последней миграции
npm run db:migrate:undo

# Генерация Swagger документации
npm run docs-gen

# Запуск тестов
npm test

# Проверка кода (линтинг)
npm run lint
```

## 🎯 Следующие шаги

1. **Откройте Swagger UI**: http://localhost:5001/api-docs
2. **Прочитайте полную документацию**: См. файл `API.md`
3. **Настройте IDE**: Рекомендуется VS Code с расширениями:
   - ESLint
   - Prettier
   - Node.js Extension Pack

## 💡 Советы для разработки

1. **Используйте nodemon** - уже настроен в `npm run dev`, автоматически перезапускает сервер при изменениях
2. **Проверяйте логи** - все ошибки выводятся в консоль
3. **Используйте Postman/Insomnia** - для тестирования API
4. **Читайте документацию** - Swagger UI обновляется автоматически

## 🆘 Нужна помощь?

- Проверьте файл `DEPLOYMENT.md` для более подробной информации
- Проверьте логи в консоли на наличие ошибок
- Убедитесь, что все зависимости установлены: `npm install`

---

**Готово!** 🎉 Ваш сервер должен быть запущен и готов к разработке!

